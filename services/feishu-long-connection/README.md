# 海芯博创飞书长连接服务

该服务使用飞书官方 SDK 建立长连接，接收飞书消息，交给海芯博创平台处理，再把 AI、知识库、工作流或审批结果回复到原会话。

1. 把 `.env.example` 中四项配置写入服务器环境变量。
2. 执行 `npm install`。
3. 执行 `npm start`。
4. 在飞书开放平台选择“使用长连接接收事件”，订阅 `im.message.receive_v1`。

生产环境请使用 Docker、systemd 或进程管理器保持服务常驻并自动重启。
