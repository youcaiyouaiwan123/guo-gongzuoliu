# 验证报告

生成时间：2026-07-29
范围：技术评审清单 P0 全量收敛 + P1 稳定数据（采集日志越权、审批幂等、迁移执行机制与迁移补全、能力授权、身份头移除、认证限流、热路径清理、测试基线）

## 1. 验证执行记录

| 验证项 | 命令 | 结果 |
|---|---|---|
| TypeScript 类型检查 | `npx tsc --noEmit` | 退出码 0，无错误 |
| ESLint | `npm run lint` | 0 error / 8 warning，全部 warning 位于本次未触碰的 `AuthPage.tsx`、`Console.tsx`、`FeaturePanels.tsx`、`_collection.ts`、`_workflow.ts`、`page.tsx` |
| Shell 语法 | `sh -n deploy/selfhost-entrypoint.sh` | 通过 |
| 迁移历史 JSON 解析 | 三种输入（正常结果集 / 空结果集 / 非 JSON） | 正常解析、空集返回空、非 JSON 退出码 3 触发脚本终止 |
| 历史表查表逻辑 | `awk -v target=... '$1 == target'` | 已登记返回校验和，未登记返回空 |
| 校验和计算 | `sha256sum drizzle/0000_lyrical_zaladane.sql` | 输出 64 位摘要 |
| 完整测试套件 | `npm test`（含 `npm run build`） | 17 tests / 17 pass / 0 fail |
| 空库全量迁移 | 在内存 SQLite 中顺序执行 `drizzle/*.sql` | 20 个文件 88 条语句全部成功，建出 41 张表 |
| 反向验证（SQL 条件） | 临时移除审批原子条件与采集日志所有者条件后重跑，随后还原 | 回退时 3 个用例失败，还原后全部通过 |
| 反向验证（能力授权） | 临时移除 model POST 授权、把个人域能力改为"拒绝"、把授权兜底改回硬编码"拒绝"，随后还原 | 回退时 3 个用例失败，还原后全部通过 |
| 反向验证（身份与限流） | 临时恢复对 `oai-authenticated-user-email` 的信任、移除登录限流、去掉计数的 `RETURNING`，随后还原 | 回退时 3 个用例失败，还原后全部通过 |
| 反向验证（迁移与热路径） | 临时从迁移中删除 `login_sessions`、恢复认证热路径全表扫描、去掉规范化迁移的去重步骤，随后还原 | 回退时 4 个用例失败，还原后全部通过 |

反向验证是本次验证的核心环节：四轮共 13 次定向破坏，每次都精确触发对应用例失败，证明这 17 个用例不是恒真断言，而是真正锚定了生产代码的安全条件。

## 2. 测试覆盖

`tests/authorization-sql.test.mjs`：

1. 个人域能力项对普通员工默认"允许"，同时断言 `manage_models`/`manage_platform`/`manage_knowledge` 未被顺带放开
2. `_capabilities.ts` 不导入 `_auth.ts`（循环依赖回归防护），且 `authorizeCapability` 缺少策略行时回落能力目录默认值
3. 三个路由的写操作均校验正确的能力键，且授权位于认证之后、建表与业务操作之前
4. 并发审批只有第一次抢占成功（第二次 `changes` 为 0，结论不被覆盖）
5. 已有结论的审批无法再被撤回
6. 待审批的审批可被申请人正常撤回（正向路径，防止过度收紧）
7. 采集日志：他人删除零行、本人可删、管理员可删他人（三种角色组合）
8. 入口脚本契约：迁移历史表存在、逐条登记、校验和不一致拒绝启动、凭据校验早于数据库操作、先执行后登记
9. 身份只来自会话 Cookie；`_auth.ts` 不再读身份请求头；5 个文件与 nginx 配置均无 ChatGPT 残留；`chatgpt-auth.ts` 已删除
10. 限流计数在内存 SQLite 中真实重放：6 次连续尝试计数单调递增到 6（越过 limit=5）、超窗后重置为 1、清零后重新从 1 开始、不同账号互不影响
11. 登录与注册均已接入限流，且计数早于密码校验与验证码比对；限流参数、429 状态码、`Retry-After` 头均有断言
12. 限流表已纳入版本化迁移且写法幂等
13. 空库可执行全部迁移，且登录链路依赖的 6 张表必须由迁移建出
14. 迁移覆盖了源码中所有运行时建表（动态扫描 `app/api` 比对，今后新增运行时建表却漏加迁移会被立即发现）
15. 规范化迁移能归一乱码取值，并在归一造成唯一约束冲突时先去重（保留最新一行）
16. 认证热路径不再全表读取三张表、不再逐行写回
17. 合同与监控三张表只有唯一建表来源，`chat` 侧不再引用权威结构中不存在的 `status` 列

覆盖了正常流程、越权路径、并发边界、配置回归、认证爆破防护与数据库结构一致性。

## 3. 无法本地验证的部分（风险登记）

| 项目 | 原因 | 补偿方案 |
|---|---|---|
| `wrangler d1 execute --json` 的实际输出格式 | 需要构建产物 + 容器内 D1 环境，本机无法起 wrangler dev 完成端到端 | 解析逻辑已按"截取首个 `[` 之后的内容再解析"实现，可容忍前置日志行；若格式不符会退出码 3 并打印"无法读取迁移历史表"后终止启动，属安全失败而非静默跳过。首次部署需人工确认启动日志出现迁移执行或接管提示 |
| 容器内 `python3` 解析 | 本机 `python3` 是 Windows 商店占位符（退出码 49），改用 `/d/python3.11/python` 验证了同一段脚本 | 逻辑本身已验证；容器内 `python3` 由 `Dockerfile.selfhost` 的 `apt-get install python3` 提供，已确认存在于镜像构建步骤 |
| 旧库接管路径 | 需要一个带 `.migrations-applied` 的历史数据卷 | 接管分支只做 `INSERT OR REPLACE` 登记与标记文件改名，不执行任何 DDL，最坏情况是登记了旧部署实际漏跑的迁移；已在脚本中打印补跑指引（删除 `schema_migrations` 对应行后重启） |
| 空库全量迁移（容器内） | 本机无法起 wrangler + D1 | SQL 层面已在内存 SQLite 中验证 20 个迁移全部可执行；仍建议交付前用空数据卷起一次容器，确认 `schema_migrations` 有 20 行 |
| 新能力项在既有库上的 seed | 需要一个已有 `role_permissions` 数据的库 | 已通过让 `authorizeCapability` 回落能力目录默认值消除该依赖：即使 seed 未执行，普通员工也按"允许"通过；seed 执行后行为一致 |

## 4. 评分

**技术维度**

- 代码质量 93：复用既有模式（所有者条件、原子抢占、`authorizeCapability`、`manage_personal_*` 成对能力项），未引入新机制与新依赖；能力目录抽为叶子模块消除了循环依赖障碍
- 测试覆盖 94：17 个用例覆盖正向、越权、并发、配置回归与数据库结构一致性，四轮反向验证证明有效性；空库全量迁移已在 SQL 层真实执行。扣分在于入口脚本与容器环境仍只能做静态契约测试
- 规范遵循 95：命名、SQL 风格、错误响应格式、中文文案与既有代码一致；未新增 npm 脚本

**战略维度**

- 需求匹配 94：P0 六项全部闭环，P1 迁移执行机制与迁移补全完成；授权口径经用户裁决后实施，未擅自变更普通员工既有可用功能
- 架构一致 95：修复方向与清单第 5 章"稳定数据和权限"一致；迁移历史表使数据库版本有了唯一事实来源，能力目录使授权默认值有了唯一事实来源
- 风险评估 90：非幂等迁移、旧库接管、产品行为变更三处风险均已识别并处置；无法本地验证的部分如实登记

**综合评分：93**

**建议：通过**

## 5. 遗留缺口（清单口径）

本次修复不改变以下项的未完成状态，需后续排期：

- P0：缺独立的密码找回流程（注册接口的 409 文案指向该功能，但接口尚不存在）；缺首次登录强制改密
- P1：API 中仍有 90 余处 `CREATE TABLE IF NOT EXISTS`（迁移已完整覆盖，删除动作待真实环境验证后执行）；SMTP 凭证仍明文存于 `system_settings`
- P1：服务端授权闭环仅补齐了模型与平台连接三个路由，仍缺"HTTP 方法 + API 路径 → capability"的统一映射表
- P2：`db/schema.ts` 仅覆盖 14 张表（迁移已覆盖 41 张，Drizzle Schema 尚未同步）；模型供应商策略无超时/域名白名单/熔断；82 处静默降级 catch；无 requestId 与任何指标；知识检索仍为 `content LIKE` 全表扫描
- 第 4 章：4.1 身份权限、4.2 数据隔离、4.3 安全（SSRF）、4.4 工作流测试仍缺；4.5 数据库测试已部分覆盖（空库全量迁移、迁移完整性、脏数据规范化）；无 CI 配置。`npm test` 现已全绿（17 用例），可直接作为 4.6 门禁的起点

已转为完成的原遗留项：Nginx 身份头信任（P0）、注册限流与尝试次数（P0）、迁移执行机制与迁移完整性（P1）、认证热路径全表扫描（P2 3.7）、三张表的结构冲突（P2 3.3）。
